Возникла проблема с компиляцией шаблонов, очень похоже на баг.
При попытки компилирования двух шаблонов с одинаковым именем в одну дирректорию, сохраняется только один скомпилированный файл.

Например:

Путь к первому шаблону app/templates/test.tpl
Путь ко втрому шаблону app/templates/modules/test.tpl

Дирректория для компиляции app/cache/templates

Если попробовать отобразить оба шаблона, скомпилируется и сохранится только 1 из них.
